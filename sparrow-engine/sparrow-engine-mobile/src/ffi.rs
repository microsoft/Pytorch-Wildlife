//! Placeholder FFI module for the mobile flavor.
//!
//! P2.1 does not expose C ABI functions yet. The file exists so the optional
//! `ffi` feature can run the same cbindgen/csbindgen build path as CPU/GPU.
