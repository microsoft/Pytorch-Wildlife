# Inquisitor approvals — round 1

Method: opened every cited source location with `view` and grepped for adjacent
context; verified each plan claim against actual code. Approvals below are
independent of the plan authors' summaries.

## Auditor verdicts

ITEM-AUD-001 | APPROVED — Verified three byte-identical `while offset < total_samples { … }` blocks at CPU detect_audio.rs:372-381 and :551-560, plus the already-named `compute_segment_offsets` at GPU models/audio.rs:1316-1332. Lifting into `preprocess_audio.rs` is the right home — both CPU and GPU already depend on core, and the GPU function's doc comment is literally `Mirror of sparrow-engine-cpu`, an explicit drift admission. Three-site rule satisfied. The new helper must keep the inclusive-tail termination (last offset emitted then break when `remaining <= segment_samples`) — that ordering is load-bearing for tail-padded windows.

ITEM-AUD-002 | APPROVED — Verified three identical `start_s` / `actual_end` / `end_s` blocks at CPU detect_audio.rs:487-489 + 654-656, GPU models/audio.rs:1398-1400, all using the same `(seg_offset + segment_samples).min(total_samples)` clamp. Co-locating with ITEM-AUD-001 keeps timing math in one module. Signature `(seg_offset, segment_samples, total_samples, sample_rate) -> (f32, f32)` is right; do NOT collapse the `min(total_samples)` semantics (it's the tail-padding contract).

ITEM-AUD-003 | APPROVED — Confirmed columns: parent fields `start_time_s`/`end_time_s`/`confidence` at column 16, `classes: Vec::new(),` lines 222/228/234 at column 12. Pure rustfmt-equivalent fix. Trivial, correct.

ITEM-AUD-004 | APPROVED — Confirmed CPU detect_audio.rs:217-220 `RawAudio { sample_rate: _, window_samples }`, while line 152 already bound `sample_rate` from the outer match on the same enum. The `sample_rate: _` is dead pattern noise; switching to `..` is idiomatic. Pure readability.

ITEM-AUD-005 | APPROVED — Confirmed softmax (lines 685-698) and top_k_indices (700-712) have no doc comment. The type-incompatibility argument is real: `Classification { label_id, label, confidence }` vs `AudioClass { class_idx, label, probability }` cannot share a primitive without splitting one of them. A 2-line doc that references `sparrow_engine_core::postprocess::try_softmax` is the right tripwire against a well-meaning dedup PR. Suggest the doc also note that the duplication is tracked under cross-scope finding #2 in the plan, so a future split of `softmax_probs` is signalled.

## Reviewer verdicts

ITEM-REV-001 | APPROVED — Verified manifest.rs:644-674 RawAudio block runs BEFORE inference parsing at :756, so the `if let Some(seg_dur) = raw.inference.segment_duration_s` consistency check (line 664) silently skips for `strategy = single | tiled` audio manifests where `segment_duration_s` is `None`. Also verified line 783/788 use only `<= 0.0`, so `+inf` passes parsing and would reach runtime. The proposed fix (require sliding_window for audio, reject non-finite, then run the consistency check unconditionally) is correctly scoped. Acceptance test list is adequate (valid sliding_window, missing/non-sliding strategy, mismatch, ±1 tolerance, non-finite). Don't forget: golden manifests in `sparrow-engine/models/*.toml` must already use sliding_window — recommend the fix include a `grep -L sliding_window sparrow-engine/models/*audio*.toml` check during editing to confirm no in-tree audio manifest regresses.

ITEM-REV-002 | APPROVED — Verified model_type.rs:24-39: today both (RawAudio,Sigmoid) and (MelSpectrogram,Softmax) derive to AudioDetector/AudioClassifier respectively, but the runtime path in CPU detect_audio.rs dispatches on `preprocess_method` only (line 183), so RawAudio+Sigmoid silently runs `detect_audio_loop_raw` which applies softmax + top-K — wrong activation. Manifest-level rejection BEFORE derive_model_type is correct. The model_type.rs unit tests at 274-281 (`audio_detector_when_raw_audio_plus_sigmoid`) and 110-121 (mel+softmax) currently advertise the now-rejected combos as valid; they must be inverted to assert rejection at manifest parse. NOTE: this item is the right boundary to enforce the supported matrix — manifest is the single source of truth and runtime branches will no longer need defensive fallbacks.

ITEM-REV-003 | MODIFY — The bug is real and confirmed at the V2 builders (CPU ffi.rs:640, GPU ffi.rs:649): `combined.header.data = combined._owner.segments.as_ptr();` is unconditional, and `Vec::as_ptr()` on an empty Vec returns a dangling-but-non-null sentinel (NonNull::dangling()) — so V2's invariant "len=0 ⇒ data=null" is violated. BUT: the plan's cited line numbers (528/537) point at the V1 builders, which have the IDENTICAL bug at CPU:554 and GPU:563. The V1 builders predate Perch 2 but still ship, and downstream C consumers reading the V1 ABI for empty results will hit the same dangling sentinel. MODIFY: apply the null-on-empty convention to BOTH V1 and V2 audio result builders (4 sites total: CPU V1 line 554, CPU V2 line 640, GPU V1 line 563, GPU V2 line 649). The nested `classes` array in V2 already does this correctly (CPU 608-613, GPU 617-622) — use that as the model. Unit tests must cover both V1 and V2 empty-segments paths.

ITEM-REV-004 | APPROVED — Verified integration_ffi_symbols.rs:33-36 only pins five legacy V1 symbols (`engine_new/free`, `last_error`, `free_string`, `health`); V2 audio symbols are absent. Verified line 131 uses `expected.iter().filter(|s| !actual.contains(*s))` — strictly ⊆ check, so accidental extra exports pass. Pinning `sparrow_engine_detect_audio_v2` and `sparrow_engine_audio_result_v2_free` is the minimum tripwire; making the nm check assert equality (symmetric difference == 0) closes the extra-export hole. The 34-count assertion at line 142 already exists, so don't drop that.

ITEM-REV-005 | APPROVED — Verified integration_perch2.rs ends at the Rust API (`sparrow_engine::detect_audio::detect_audio`, line 97) and never exercises the C ABI. An `#[ignore]` + `#[cfg(feature = "ffi")]` test that mirrors the existing bundle-resolution stub and then calls `sparrow_engine_detect_audio_v2` + frees via `sparrow_engine_audio_result_v2_free` is the right shape. MUST assert: 2 segments, classes_len=5, top-1 probability == segment confidence (parity with the Rust path), and free-after-use does not leak (just exercising the free is fine; valgrind is out of scope).

ITEM-REV-006 | APPROVED — Verified integration_reexports.rs:90-95 constructs AudioSegment with `classes: Vec::new()` but never references the `AudioClass` type. Adding a `let _ac: sparrow_engine::AudioClass = sparrow_engine::AudioClass { class_idx: 0, label: None, probability: 0.0 };` line in the audio test (around line 95) is the right shape. Cross-check: confirm crate root `pub use` actually re-exports `AudioClass` before adding the test (otherwise the test will fail; if it does, ITEM-REV-006 becomes a real source fix, not just a test).

ITEM-REV-007 | APPROVED — Verified lib.rs:1608-1628 covers only the single-class labeled path. Multi-class order, None-label, empty-classes, and confidence==top1.probability cases are all missing. Pure test addition. Low risk.

ITEM-REV-008 | APPROVED — Verified response.rs:218-258 covers three cases (empty classes, single-class binary, multi-class) but every class uses `Some(label)` (the `audio_class` helper at line 210 hardcodes `label: Some(...)`). Missing case: a multi-class segment where one or more classes have `label: None` must still serialize `class_idx` and `probability`. Add a fourth test with a mix of `Some` and `None` labels and assert the JSON shape preserves the optional-label semantics additively.

ITEM-REV-009 | APPROVED — Verified main.rs:1558-1565 + 1607-1622: when `--visualize`, `inference_threshold` forces 0.0, then `output_view` post-filters by `output_threshold` which defaults to `handle.audio_confidence_threshold().unwrap_or(0.5)`. For Perch 2 (Softmax, no manifest threshold) the non-viz path runs with `threshold = 0.0` in `prepare_audio_detection` (line 174 sets `Softmax => 0.0`) and emits every window; the `--visualize` path would lower inference to 0 (no-op for softmax which is already 0) BUT then post-filter outputs at 0.5, silently dropping low-probability windows from JSON/CSV/merged-range output. That IS a "viz changes machine-readable output" surprise. The fix policy must distinguish: (a) sigmoid detector with a manifest threshold → keep post-filter behaviour (the original intent), (b) softmax classifier OR no-threshold raw audio → skip the post-filter entirely (no threshold semantics to restore). Acceptance: a CLI-level helper that returns `Option<f32>` for "should post-filter threshold" is the right shape; thread it through to the `if args.visualize` branch. Add a unit test on the helper itself (no Engine needed).

## Cross-scope findings (deferred to round 2)

Both plans flag deferred items consistent with each other (merge_segments duplication CPU↔GPU, RawAudio segment_duration_s override semantics, rank-2 logits validation, Python `__init__.py` AudioClass re-export, server `audio.rs` drift labels). The reviewer plan already proposes appending two files to the ledger for round 2 — that is the correct anti-narrowing posture (APPEND only, never narrow). No round-1 action needed from me; the lead must ensure the round-1 SCOPE_LEDGER is not mutated and that the round-2 ledger expansion records the appends with `added_round: 2`.

## Items the plans correctly chose to skip

- viz.rs / audio_heatmap_e2e.rs / classifier.rs / models/audio.rs (auditor's "files not modified" section): I checked each rationale against the source. The Perch 2 surface in those files is either intentionally minimal (viz uses only `confidence`, tests are fixtures, classifier rejection arms are defense-in-depth) or already covered by other items (audio.rs rejection mirrors detect_audio.rs validation, which is touched elsewhere). Defer is correct.
- CPU/GPU classify.rs, detect.rs, CPU preprocess.rs, types.rs (reviewer's "no planned fix"): I confirmed these files have no RawAudio-specific code paths that the reviewer's behavioural lens would touch; the audio guard in dispatch already lives in `detect_audio.rs`.

STATUS: APPROVALS-DONE
